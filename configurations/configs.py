from configurations.logger import get_logger


logger = get_logger("configs")

def is_within_roots(path):
    
    try:
        
    except Exception as e:
        logger.error(f"Error in is_within_roots: {e}")
        